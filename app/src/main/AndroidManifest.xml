<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      🔴 Desde Android 11 (targetSdk 30+) una app NO puede ver ni ligarse a un
      servicio de otro paquete si no lo declara aquí. Sin esto, bindService a la
      impresora integrada de los POS Sunmi falla EN SILENCIO —ni excepción ni
      log— y la impresora simplemente nunca aparece al buscar.
    -->
    <queries>
        <package android:name="woyou.aidlservice.jiuiv5" />
    </queries>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Bluetooth printing -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />

    <!-- USB printing (Epson TM-m30III et al. plugged in by cable). Not required so
         phones without USB-host (and PAX terminals) keep installing normally. -->
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <!-- Counter POS hardware (Sunmi D3 Pro/T3, etc.) often lacks a camera, autofocus,
         or Bluetooth. Without these, the CAMERA/BLUETOOTH permissions above make Play
         mark the app as incompatible on those devices. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />

    <!-- 🔴 DISTRIBUCIÓN: estos dos los IMPLICA Play a partir de permisos, y sin
         declararlos opcionales EXCLUYEN hardware de mostrador del catálogo
         ("Esta app solo está disponible para tus otros dispositivos").
         - wifi: lo implican ACCESS_WIFI_STATE + CHANGE_WIFI_MULTICAST_STATE, que
           usamos SOLO para descubrir impresoras por mDNS (NsdManager). mDNS
           funciona igual sobre Ethernet, y la app opera sin descubrimiento
           (impresora USB o por IP), así que un POS solo-Ethernet debe poder
           instalarla.
         - fingerprint: lo mete androidx.biometric (USE_FINGERPRINT). El login
           biométrico es opcional — hay contraseña y passkey. -->
    <uses-feature android:name="android.hardware.wifi" android:required="false" />
    <uses-feature android:name="android.hardware.fingerprint" android:required="false" />

    <application
        android:name=".AvoqadoApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="${appLabel}"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Avoqado">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Avoqado">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Plugging in a USB receipt printer offers/opens Avoqado (Square-style
                 "¿Deseas permitir que Avoqado acceda a TM-m30III?") and implicitly
                 grants USB permission for that device. -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/usb_printer_filter" />
        </activity>
    </application>
</manifest>
