# Auditoría UX Android — 2026-07-09 (79 hallazgos, 6 dominios)
Espejo de la auditoría iOS ya ejecutada (4 tandas, commits fb2a09a→cd87150 iOS main).
Plan founder-aprobado: ejecutar mismas tandas. TODO en app/src/main/java/com/avoqado/pos/.

## TANDA 1 — DINERO (ejecutar primero)
1. DOBLE COBRO efectivo Y tarjeta: PaymentFlowViewModel.kt processCashPayment:579 + confirmPayment:373 sin guarda de reentrada → doble tap = 2 createOrder + 2 pagos. Fix: flag isProcessingPayment checked-and-set síncrono al tope, reset en Success/Error/queued. + disable UI: PaymentMethodSelectionScreen.kt:159-210 presets/terminal row, TerminalSelectionScreen.kt:95.
2. SIN IDEMPOTENCY KEY: OrderRepository.kt recordCashPayment:297/recordFastCashPayment:247/createOrder:150 sin key → retry() (VM:838) tras error-de-red-pero-registrado = segundo pago. Fix: key por SESIÓN de pago minted en VM, enviado en pay/fast (backend ya la acepta, iOS la manda); TerminalPaymentService.kt:85 reusar la key (hoy requestId nuevo por intento).
3. CANCEL HIJACK: VM cancel():856 no cancela Job de sendPaymentToTerminal (310s timeout) → resultado tardío marca Success e IMPRIME recibo+comanda de pago cancelado. Fix: retener Job + cancel + generation guard antes de mutar state/imprimir (patrón iOS terminalSendGeneration).
4. SAVED CART pierde packId: CartViewModel.kt save:512-516 (CreditPack→null) / restore:738-745 → membresía restaurada cobra sin otorgar y bypassa el gate de cliente. Fix: SavedCartItem.packId + restaurar CartItemType.CreditPack. TAMBIÉN persistir reservationId + cliente (SavedCart.kt:6-14) y limpiar estado referral en restore (:738-775, descuento REFERRAL sin validación).
5. SPLIT + PACK sin grant: CheckoutScreen.kt:672-693 solo else-branch otorga. Fix: gate temprano (mismo que iOS): si hasCreditPack → bloquear split con diálogo.
6. GRANT sin retry: CustomerCreditsViewModel.grantPacks:65 ignora Booleans. Fix: PendingGrantQueue Kotlin (SharedPreferences/DataStore persistido, drain en foreground+launch, venue-scoped) — espejo del PendingGrantQueue.swift de iOS.
7. CartCreditsCard.kt:45: fetch fallido = "sin créditos". Fix: estado error + reintentar (espejo iOS).
8. KDS MOCKS: KDSViewModel.kt:85 loadMockOrders en init + advanceStatus/bump disparan API real con IDs mock. Fix: quitar (o BuildConfig.DEBUG+flag), empty state real.
9. CAJA: CashDrawerViewModel.kt:123-173 CERO canal de error (4 ops solo Log.e); CashDrawerScreen.kt:216/391 reporte de cierre FABRICADO client-side aunque falle. Fix: error StateFlow + render; reporte solo tras éxito local confirmado, desde la sesión retornada. Repo:158-408 API swallow → surfacear "pendiente de sincronizar" (scope mínimo: error flow).
10. CONTEO confirmCount InventoryViewModel.kt:497-548: resultados descartados, SIEMPRE cierra como éxito y borra datos. Fix: gate en éxito total, error + mantener review si falla. + FULL count: items no visitados = counted 0 → CERO stock silencioso (:471-484): warning "N sin contar" antes de confirmar.
11. RESERVAS: CreateReservationScreen.kt:83 solo onSuccess (falla silenciosa); CreateReservationViewModel.kt:235 promote ignorado → doble reserva. Fix: onFailure + surfacear; promote fallido marca/alerta.
12. REFERRAL capturado al INTENTO de cobro (CheckoutScreen.kt:311/523) no al éxito → pago cancelado deja referido PENDING persistido. Fix: mover captura a onComplete success (como iOS), con orderId real si está.

## TANDA 2 — ERRORES SILENCIOSOS / DISMISS-SIN-AWAIT
13. Articles forms dismiss-without-await (pierden formulario): ProductDetailView.kt:142, CategoryFormSheet.kt:80, DiscountFormSheet.kt:120, CouponFormSheet.kt:87, CreditPackFormSheet.kt:126, ModifierGroupFormSheet.kt:153. Patrón correcto existente: CreateOptionSheet.kt:58 (onSuccess callback). Fix: onSuccess param en cada mutación del VM.
14. Inventory sheets: snackbar detrás del ModalBottomSheet (CreateTransferSheet:227, CreatePurchaseOrderSheet:395, ReceiveStockSheet:167). Fix: error inline en cada sheet.
15. ArticlesRepository.kt:99 _errorMessage nunca se resetea → 2º fallo idéntico no re-dispara snackbar. Fix: clearError tras mostrar.
16. CreateProductView.kt:143 quick-create POS falla solo con Log.e. Fix: surfacear.
17. Estimates: error detrás del modal (EstimatesScreen:282 vs CreateEstimateSheet). Fix: error in-sheet.
18. Redeem sin confirm + fallo silencioso: CustomerCreditsSection.kt:111 + CartCreditsCard.kt:86 + VM redeem:71. Fix: confirm dialog (patrón pendingPack existente) + error visible. Ídem sell/redeem sin role gate (:41-131): gate canManageCustomers.
19. CustomersScreen.kt:175/417 crear cliente sin gate (checkout sí lo tiene). Fix: gate canManageCustomers.
20. 403 nunca visibles: ForbiddenInterceptor→ErrorNotifier.forbiddenError sin collector. Fix: collect en scaffold raíz + snackbar.
21. SESIÓN ZOMBIE: TokenRefreshAuthenticator clearSession() pero AppState._isLoggedIn no reactivo → app rota hasta force-restart. Fix: evento de invalidación colectado por AppState (con guard para no abortar pago en curso).
22. 401 concurrentes descartados (TokenRefreshAuthenticator:42-45 return null). Fix: esperar el refresh en curso y reintentar con token nuevo.
23. Venue switch sin rollback (AuthRepository:126 commit primero, fetches tragan errores). Fix: fetch esencial antes de commit o rollback + error.
24. Cancel/Reschedule sheets: destino nav propio → OTRA instancia de ReservationDetailViewModel → error invisible (CancelReservationSheet.kt:33, RescheduleSheet.kt:48). Fix: leer/mostrar state.error dentro del sheet.
25. runAction sin lock global (ReservationDetailViewModel:57): dos acciones concurrentes sobre la misma reserva. Fix: early-return si pendingAction != null + disable pills.
26. NotificationsViewModel:26-44 refresh/markAsRead sin error surface.
27. TipSelectionSheet dismiss (swipe) = propina 0 silenciosa (PaymentFlowScreen.kt:90-97). Fix: dismiss no comitea; requerir acción explícita.
28. Cash-drawer recordCashSale VM:867 Log.d en fallo real de insert. Fix: distinguir no-drawer de fallo.
29. cancelOrder tragado (VM:860 + OrderRepository:220 sin log). Fix: fold + log + surfacear orden abierta huérfana.

## TANDA 3 — VALIDACIÓN TEMPRANA
30. TERMINAL PREFETCH (insignia): fetchOnlineTerminals solo en selectPaymentMethod(CARD) — DESPUÉS de rating+tip (VM:288-371). Fix: probe al abrir el flujo/método; opción tarjeta disabled con razón+reintentar (espejo iOS; fail-open en error de red).
31. Transfer same-location: CreateTransferSheet.kt:230 sin check from!=to (y free-text → ubicaciones fantasma). Fix: validar from!=to (case-insensitive).
32. Email débil: SignInViewModel.kt:31 contains("@"). Fix: Patterns.EMAIL_ADDRESS.
33. Reserva sin servicios bookeables: aviso al entrar (CreateReservationScreen:123).
34. Class seed clobber reservationId (CartViewModel:289) — mismo iOS: warn/refuse si carrito ya lleva otro reservationId.
35. Inscribir-y-cobrar sin rollback (ClassSessionDetailViewModel:127): mismo criterio iOS = aviso al vaciar/abandonar (no auto-rollback).

## TANDA 4 — PULIDO
36. Vaciar carrito sin confirm (CartPanelView:416): confirm dialog (+ aviso clase inscrita).
37. Pack tile doble-tap (CartViewModel addCreditPack:369): debounce 1.5s (espejo iOS).
38. Cobrar sin debounce (PrimaryButton + CartPanelView:331): gate con showPaymentFlow/isSubmitting.
39. POS quick-create sin unit (CreateProductRequest sin campo): añadir unit picker o pasar unit; Articles form OK (pero solo manda unit si trackInventory — quitar esa condición).
40. Unidades en conteos: StockCountItem sin unit (dropped en VM:407) → agregar + render junto a cantidades; .toInt() truncations (StockCountingView:673,919; CreateTransferSheet:448) → formatQuantity.
41. PO/transfer/receive cantidades Int-only (Repository:51-88) — kg/L imposibles de ordenar/mover/recibir. Fix: Double + input decimal (VERIFICAR backend acepta decimal antes).
42. Conteo sin BackHandler ni confirm en Cancelar (StockCountingView:89): BackHandler + "¿Descartar conteo?".
43. Scanner: sin torch (BarcodeScannerView:141) — añadir toggle; permiso denegado permanente sin deep-link a Settings (:205).
44. SetupWizardScreen.kt:61-87 COMPLETAMENTE FALSO (hardcoded "2 de 5") + chevrons sin onClick (:268-327). Fix: derivar estado real (patrón SetupChecker iOS) + wire navegación o quitar chevrons.
45. Permisos/PIN placeholders sin señal (MoreMenuScreen:804): badge "próximamente" o esconder.
46. Caja/Pedidos/KDS sin role gate (MoreMenuScreen:434-459): gates consistentes con Informes/Artículos.
47. 3 instancias CustomerCreditsViewModel desincronizadas (CheckoutScreen:98, CartCreditsCard:38, ProductGridView:76): hoist balances al repo singleton.
48. BIOMÉTRICO MUERTO: saveBiometricCredentials() sin callers — inscribir tras login exitoso (opt-in) o borrar UI muerta. (Feature, no bug — proponer al founder.)
49. Estimates detail read-only (sin convertir a venta — gap de paridad vs iOS, ya en backlog) + selectedEstimate stale tras delete en tablet.
50. Métricas: totalQuantity mezcla unidades como "uds"; valor truncado a Int (InventoryMetricsView:389,119).

## OK verificados (no tocar): cash honra tip/rating (gating pre-método, method-agnostic); scanner debounce+not-found; timeclock errores visibles; reports plan-gated; login preserva campos; PO/receive validación temprana; onboarding sin trampas de permisos; mode-switch nav OK.
